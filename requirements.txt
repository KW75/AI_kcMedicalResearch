# requirements.txt - local development and Docker installs
#
# Shared deps live in requirements-base.txt. This file adds developer tools
# and platform-specific packages.
#
#   Render : requirements-render.txt  (standalone, pinned)
#   CI     : requirements-ci.txt
#   OCR    : requirements-ocr.txt     (optional, see note below)
#
# Install:  pip install -r requirements.txt

-r requirements-base.txt

# --- Developer tooling ---
pytest>=7.0.0
pytest-cov>=4.0.0

# --- Windows-only ---
# Legacy .doc support. pywin32 306 does not build on Python 3.13+.
pywin32>=312; platform_system == "Windows"

# --- OCR is OPTIONAL and NOT installed here -------------------------------
# document_reader.py and rag.py import pytesseract / easyocr / cv2 /
# pdf2image inside try/except and degrade gracefully when absent.
#
# The OCR stack pulls PyTorch via easyocr (~2 GB) and also needs system
# binaries that pip cannot install (Tesseract, Poppler).
#
# To enable OCR:
#     pip install -r requirements-ocr.txt
# plus the system binaries listed in that file.
