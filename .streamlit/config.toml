# .streamlit/config.toml

[theme]
base = "dark"           # "dark", "light", or "auto"
baseFontSize = 18       # Font size in pixels

[server]
runOnSave = false       # Don't auto-reload on file save

[browser]
gatherUsageStats = false  # Opt out of anonymous usage stats